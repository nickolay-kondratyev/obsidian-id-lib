{
	"name": "obsidian-id-lib",
	"version": "0.1.0",
	"private": true,
	"description": "Shared doc-id read/ensure for Obsidian plugins (md frontmatter + canvas) with cross-plugin write serialization.",
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"check": "tsc -noEmit"
	},
	"license": "0-BSD",
	"peerDependencies": {
		"obsidian": "*"
	},
	"devDependencies": {
		"obsidian": "latest",
		"typescript": "^5.8.3",
		"vitest": "^4.1.10"
	}
}
